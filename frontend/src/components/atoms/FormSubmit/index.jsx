import './index.css'

const FormSubmit = ({ content }) => {
    return (
        <button type="submit" className="cyber-form-submit">
            {content}
        </button>
    );
}
export default FormSubmit;